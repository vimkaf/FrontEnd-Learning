/**
 * Create a function that takes an array of email addresses and prints which ones are valid and
 * which ones are not. For simplicity,
 * consider an email valid if it contains "@" and a dot (".") after "@".
 * const emails = ['test@example.com', 'invalid-email.com', 'user@domain', 'name@domain.com' , 'b@g.com'];
 * validateEmails(emails);
 */
